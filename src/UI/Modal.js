import React from 'react';
import styles from './Modal.module.css';

function Modal() {

    const modalHandler = () => {
        
    };

    return (
        <div className={styles.modal} onClick={modalHandler}>
            
        </div>
    )
}

export default Modal;
